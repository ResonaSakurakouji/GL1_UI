{
    // 运行之前先npm start
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "react调试",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/gl1ui_rts_n16/src"
        }
    ]
}